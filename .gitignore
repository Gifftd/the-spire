# Local backups taken before edits — not for git history
backups/

# Local-only AI/dev context — kept off GitHub on purpose
CLAUDE.md

# Third-party copyrighted content (book scrapes, normalized bestiaries, etc.) —
# stays in KV only, never committed to the public repo. Imported into the worker
# by the DM.
obojima-seed.json
obojima-potions.json
dnd-potions-seed.json
obojima-affinity-tags.json
*.seed.json
mm2024.json
mm-*.json
bestiary.json
bestiary-*.json

# macOS
.DS_Store

# Editor / IDE junk
.vscode/
.idea/
*.swp
*.swo

# Node (in case any tooling appears later)
node_modules/
